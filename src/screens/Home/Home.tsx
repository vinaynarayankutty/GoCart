import {View, Text} from 'react-native';
import React from 'react';
import {fonts} from '@theme/fonts';

const Home = () => {
  return (
    <View>
      <Text style={{fontFamily: fonts.ManropeBold}}>Home</Text>
    </View>
  );
};

export default Home;
